# RALLIVIO

Creator-economy discovery platform.

## Current development

The active development branch is `feature/verify-pipeline`. It contains the repository verification pipeline and the first live Discover UI QA surface.

## Commands

```bash
npm install
npm run verify
npm run dev
```

The current Discover surface is a UI QA build. Production integrations and real data are added only through the canonical specifications and approved scope.
