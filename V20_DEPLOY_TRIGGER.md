# V20 deployment trigger

Environment variables for the V20 preview deployment were updated in Vercel. This file intentionally triggers a fresh deployment so the new Preview configuration is picked up.
